#include "hephInstance.hpp"

HephResult	HephInstance::destroy() {
	return (HephResult());
}

HephResult	HephInstance::createInstance(HephInstanceCreateInfo& createInfo) {
	return (HephResult());
}

HephResult	HephInstance::createDevice(HephDeviceCreateInfo& createInfo) {
	return (HephResult());
}
